// ----- Debugging function switches

// Logs out raw health API data
#define DEBUG_HEALTHAPIS 0

// Logs out raw heart rate data
#define DEBUG_HEALTHAPIHEARTRATE 0

// Disables persistence and generates dummy data
#define DEBUG_READ_DUMMY_DATA 0

#define DEBUG_PERSISTED_DATA 0

// Do not read persisted data irrespective of whether
// it exists or how up-to-date it is
#define DEBUG_IGNORE_PERSISTED_DATA 0